<nav class="navbar navbar-expand-sm navbar-dark bg-primary mb-3">
    <div class="container">
        {% if app.user %}
        <a href="/" class="navbar-brand">{{ app.user.username }}</a>
        {% endif %}
        <button class="navbar-toggler" type="button" datatoggle="collapse" data-target="#mobile-nav">
            <span class="navbar-toggle-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="mobile-nav">
            <ul class="navbar-nav ml-auto">
                {% if  is_granted('ROLE_USER') %}
                    <li class="nav-item">
                        <a href="" class="nav-link">Home</a>
                    </li>
                    <li class="nav-item">
                        <a href="{{ path('race_index') }}" class="nav-link">Races</a>
                    </li>
                    <li class="nav-item">
                        <a href="{{ path('animal_index') }}" class="nav-link">Animales</a>
                    </li>
                {% endif %}
                {# {% if is_granted('ROLE_ADMIN') %} #}
                {# <li class="nav-item"> #}
                {# <a href="{{ path('admin_utilisateurs')}}" class="nav-link">Administration</a> #}
                {# </li> #}
                {# {% endif %} #}

                {% if not app.user %}
                    <li class="nav-item">
                        <a href="{{ path('security_login') }}" class="nav-link">Connexion</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a href="{{ path('security_logout') }}" class="nav-link">Déconnexion</a>
                    </li>
                {% endif %}
                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item">
                        <a href="{{ path('admin_utilisateurs') }}" class="nav-link">Administration</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>